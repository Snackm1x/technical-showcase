import React, { Component } from 'react';
import ReactDOM from 'react-dom';
import { ShoppingListContainer } from './components';

ReactDOM.render(
    <ShoppingListContainer/>,
    document.querySelector('#root'));